# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# Data (large binary files — use DVC or Modal volumes)
data/raw/
data/processed/
data/external/
data/synthetic/   # regenerable via scripts/build_redaction_corpus.py

# Model checkpoints (large binary files — upload to Modal volume)
/models/

# Outputs and figures
outputs/

# Test and linting caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Jupyter notebooks (use .py scripts instead)
.ipynb_checkpoints/

# Environment files — never commit secrets
.env
.env.local
.env.*.local

# Modal secrets — never commit
modal/secrets.toml

# OS
.DS_Store
Thumbs.db

# Weights & Biases
wandb/

# Hydra output directories (if used)
outputs/
multirun/

# Type stubs generated by stubgen
*.pyi.bak

# Graphify knowledge graph — generated locally, never tracked
graphify-out/

# HF dataset export — ignore generated files, track the dataset card
datasets/synoptiq-corpus/data/
datasets/synoptiq-corpus/alignments.json
datasets/synoptiq-corpus/pericopes.parquet
*.zip
SYNOPTIQ_MASTER_PLAN.md
IMPLEMENTATION_PLAN.md
FYP_SUPERVISORS.md
FYP_PROJECTS_TRIAGE.md
FYP_SEARCH_PROMPT.md
emails/

# LaTeX papers — drafts kept local, deliberately NOT published to GitHub
paper/
paper_limit/

# Staged HuggingFace upload payloads (adapters are large; card is tracked in release/hf_cards/)
release/hf_upload/
